torch>=2.0.0
einops>=0.7.0
numpy>=1.24.0
scipy>=1.10.0
h5py>=3.8.0
matplotlib>=3.7.0
tqdm>=4.65.0
thop>=0.1.1     # optional — FLOPs/param profiling in logger.py; falls back to manual count if absent
