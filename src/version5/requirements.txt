torch>=2.0.0
einops>=0.7.0      # transitively needed by the shared "improved" (V2) baseline
numpy>=1.24.0
scipy>=1.10.0      # used by the shared dataset_loader_overlap.py this version relies on
h5py>=3.8.0        # used by the shared dataset_loader_overlap.py this version relies on
matplotlib>=3.7.0
tqdm>=4.65.0
openpyxl>=3.1.0
